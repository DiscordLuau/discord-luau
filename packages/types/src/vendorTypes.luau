--[[
	THIS FILE WAS AUTOGENERATED BY THE @types-parser/ PACKAGE;

	modifications to this file may be overwritten when the package is updated.
]]

-- [[ GENERATED CLASSES ]] --
export type Connection = {
	-- [[ CLASS FUNCTIONS ]] --
	Connected: boolean, 
	_disconnectHandler: () -> (), 
}& {
	-- [[ CLASS FUNCTIONS ]] --
	new: (() -> ()) -> (Connection),

	-- [[ CLASS METHODS ]] --
	Disconnect: (Connection) -> (Connection)
}

export type Future<T...> = {
	-- [[ CLASS FUNCTIONS ]] --
	_threads: { thread }, 
	_errorHandlers: { thread }, 
	_valueList: { any }, 
	isPending: boolean, 
}& {
	-- [[ CLASS FUNCTIONS ]] --
	new: () -> (Future<T...>),
}

export type Logger = {
	-- [[ CLASS FUNCTIONS ]] --
	logUuid: string, 
}& {
	-- [[ CLASS FUNCTIONS ]] --
	new: (() -> ()) -> (Logger), 
	setVerbose: (boolean) -> (),

	-- [[ CLASS METHODS ]] --
	_out: (Logger, string) -> (), 
	trace: (Logger, string) -> (), 
	info: (Logger, string) -> (), 
	warn: (Logger, string) -> (), 
	error: (Logger, string) -> (), 
	critical: (Logger, string) -> ()
}

export type Signal<T...> = {
	-- [[ CLASS FUNCTIONS ]] --
	_connections: { thread }, 
	_yielding: { thread }, 
}& {
	-- [[ CLASS FUNCTIONS ]] --
	new: () -> (Signal<T...>),

	-- [[ CLASS METHODS ]] --
	Fire: (Signal<T...>) -> (), 
	Wait: (Signal<T...>) -> (), 
	Connect: (Signal<T...>, (T...) -> ()) -> (Connection)
}

-- [[ MODULE RETURN ]] --
return "GENERATED_TYPES_FILE"
